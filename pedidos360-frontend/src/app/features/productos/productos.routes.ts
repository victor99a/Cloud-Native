import { Routes } from '@angular/router';
import { ProductosListComponent } from './pages/productos-list.component';

export const productosRoutes: Routes = [{ path: '', component: ProductosListComponent }];
